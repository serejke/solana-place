{
  "name": "@serejke/place/client",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "npm-install": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "client",
        "command": "npm install"
      }
    }
  }
}
